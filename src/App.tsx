import * as React from "react";
import { UserTable } from "./table";

export default function App() {
  return (
    <section
      style={{
        margin: 50,
      }}
    >
      <UserTable />
    </section>
  );
}
